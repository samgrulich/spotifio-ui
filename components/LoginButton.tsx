/** @jsx h */

import { h } from "preact";
import { tw } from "@twind";
import { Handlers } from "$fresh/server.ts";
import { Credentials } from "../modules/functions.ts";

export default function Login(props: Credentials)
{
  return (
    <div hidden={true}>Login2</div>
  );
}