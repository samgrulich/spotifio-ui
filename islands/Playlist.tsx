/** @jsx h */
import { h } from "preact";

/**
 * - Get playlist info
 * 
 * Playlist 
 *  img
 *  name
 *  description
 *  link (url of the spotify playlist)
 *  datepicker (month, year)
 * 
 * :interactable datepicker
 *  (on datepicker changed)
 *  load snapshot
 *  create new spotify playlist, 
 *   if there is not already one generated
 *  then replace its contents with the snapshot data
 * 
 * Snapshot
 *  small track count
 *  all tracks 
 *  :on track hover show name
 */

export default function Playlist()
{
    return (
        <div></div>
    )
}